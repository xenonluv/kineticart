# 키넥틱 아트 프로젝트 환경 설치 가이드

> Supabase(메타데이터 + REST API) + 집 Mac(원본 이미지/텍스트 저장) + Cloudflare Tunnel(외부 접근)
> 완전 무료 구성

---

## 0. 전체 아키텍처

```
                          ┌─────────────────────────────┐
                          │   외부 클라이언트 (웹/앱)      │
                          └───────────────┬──────────────┘
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    │                     │                     │
                    ▼                                           ▼
        ① 메타데이터 조회                              ② 원본 파일 조회
     (제목, 설명, 태그, 촬영일 등)                    (이미지 원본, 상세 설명 원문)
                    │                                           │
                    ▼                                           ▼
        ┌───────────────────────┐              ┌──────────────────────────────┐
        │   Supabase REST API    │              │   images.내도메인.com         │
        │  (PostgREST 자동 생성)  │              │        ↓ Cloudflare 엣지      │
        │  DB: 500MB 무료 한도    │              │        ↓ (아웃바운드 터널)     │
        └───────────────────────┘              │   집 Mac (24시간 구동)         │
                                                │   - cloudflared             │
                                                │   - 파일 서버 (nginx 등)     │
                                                │   - 원본 이미지 + 텍스트      │
                                                └──────────────────────────────┘
```

**핵심 아이디어**
- Supabase DB에는 **메타데이터만** 저장 (용량 작음 → 무료 500MB 한도 안에서 충분)
- 실제 무거운 원본 이미지/설명 원문 파일은 **집 Mac에 그대로 보관**
- 메타데이터 안에 원본 파일의 접근 URL(예: `https://images.내도메인.com/works/001.jpg`)을 필드로 저장
- 클라이언트는 Supabase REST API로 목록/메타데이터를 먼저 받고, 필요할 때 그 URL로 집 Mac에 직접 접근해서 원본을 받음
- 집 Mac은 Cloudflare Tunnel로 외부에 안전하게 노출 (포트포워딩 불필요, 공인 IP 불필요)

---

## 1. Supabase 설정

### 1-1. 프로젝트 생성
1. https://supabase.com 접속 → 회원가입 (GitHub 계정으로 가능)
2. **New Project** 클릭
3. 프로젝트 이름, 데이터베이스 비밀번호 설정 (비밀번호는 꼭 기록해둘 것)
4. Region은 **Northeast Asia (Seoul)** 선택 시 국내 접속 속도 유리

### 1-2. 테이블 스키마 설계

Supabase 대시보드 → **SQL Editor** → 아래 쿼리 실행:

```sql
create table kinetic_artworks (
  id uuid primary key default gen_random_uuid(),
  title text not null,
  description text,               -- 짧은 요약 설명 (긴 원문은 파일로 별도 보관)
  created_year int,
  materials text,                 -- 사용 재료 (예: 스틸, 모터, LED)
  dimensions text,                -- 크기 정보
  tags text[],                    -- 검색/분류용 태그 배열
  thumbnail_url text,             -- 작은 썸네일은 Supabase Storage에 저장 가능 (선택)
  image_url text not null,        -- 집 Mac 원본 이미지 접근 URL
  detail_text_url text,           -- 집 Mac 원본 설명 원문 파일 접근 URL
  file_size_mb numeric,           -- 원본 파일 용량 기록용 (모니터링 목적)
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- 외부에서 읽기만 가능하도록 RLS(행 단위 보안) 활성화
alter table kinetic_artworks enable row level security;

-- 누구나 조회(select)는 가능, 쓰기는 인증된 사용자(본인)만 가능하도록 설정
create policy "Public read access"
  on kinetic_artworks for select
  using (true);

create policy "Authenticated write access"
  on kinetic_artworks for insert
  with check (auth.role() = 'authenticated');

create policy "Authenticated update access"
  on kinetic_artworks for update
  using (auth.role() = 'authenticated');
```

> **왜 RLS(Row Level Security)를 켜는가?**
> Supabase는 테이블을 만들면 기본적으로 REST API가 즉시 열립니다. RLS를 켜지 않으면 누구나 데이터를 수정/삭제할 수 있으니, 위 정책처럼 "조회는 누구나, 쓰기는 인증된 사용자만" 형태로 반드시 제한하세요.

### 1-3. REST API 키 확인
1. 대시보드 → **Project Settings → API**
2. 아래 두 가지를 기록:
   - **Project URL** (예: `https://xxxxx.supabase.co`)
   - **anon public key** (클라이언트에서 조회용으로 쓰는 공개 키)
   - **service_role key** (관리자용, 절대 클라이언트/공개 코드에 노출 금지 — 서버 쪽에서만 사용)

### 1-4. REST API 호출 예시

Supabase는 테이블만 만들면 PostgREST 기반 REST API가 자동 생성됩니다. 별도 백엔드 코드 불필요.

```bash
# 전체 작품 목록 조회
curl "https://xxxxx.supabase.co/rest/v1/kinetic_artworks?select=*" \
  -H "apikey: [anon public key]" \
  -H "Authorization: Bearer [anon public key]"

# 특정 태그로 검색
curl "https://xxxxx.supabase.co/rest/v1/kinetic_artworks?tags=cs.{motor}&select=*" \
  -H "apikey: [anon public key]" \
  -H "Authorization: Bearer [anon public key]"
```

응답 예시:
```json
[
  {
    "id": "a1b2c3d4-...",
    "title": "회전하는 빛",
    "description": "모터와 거울을 이용한 반사광 설치작품",
    "image_url": "https://images.내도메인.com/works/001.jpg",
    "detail_text_url": "https://images.내도메인.com/texts/001.txt",
    "file_size_mb": 45.2
  }
]
```

클라이언트는 이 응답을 받은 뒤, `image_url`/`detail_text_url`로 **직접** 집 Mac에 접근해서 원본을 내려받습니다.

### 1-5. 프로젝트 자동 일시정지 방지 (선택사항)
Supabase 무료 프로젝트는 **7일간 API 요청이 없으면 자동 일시정지**됩니다. 방지하려면:
- 아래 3-4단계에서 만들 집 Mac 스크립트에 **6일마다 한 번 Supabase에 더미 조회 요청**을 보내는 cron 작업을 추가 (아래 4장에 포함)

---

## 2. 집 Mac 준비 (파일 서버)

### 2-1. 폴더 구조 설계

```
/Users/[사용자명]/kinetic-art-server/
├── works/              # 원본 이미지 파일들
│   ├── 001.jpg
│   ├── 002.jpg
│   └── ...
├── texts/               # 원본 설명 텍스트 파일들
│   ├── 001.txt
│   ├── 002.txt
│   └── ...
└── logs/                # 접근 로그 (선택)
```

### 2-2. 간단한 파일 서버 구동 (nginx 사용)

**nginx 설치 (Homebrew 이용)**
```bash
# Homebrew가 없다면 먼저 설치
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# nginx 설치
brew install nginx
```

**nginx 설정 파일 수정**
```bash
# 설정 파일 위치 확인 (보통 /opt/homebrew/etc/nginx/nginx.conf)
nano /opt/homebrew/etc/nginx/nginx.conf
```

`server` 블록에 아래 내용 추가/수정:
```nginx
server {
    listen       8787;
    server_name  localhost;

    location /works/ {
        alias /Users/[사용자명]/kinetic-art-server/works/;
        autoindex off;
        add_header Cache-Control "public, max-age=86400";
    }

    location /texts/ {
        alias /Users/[사용자명]/kinetic-art-server/texts/;
        autoindex off;
        add_header Content-Type "text/plain; charset=utf-8";
    }
}
```

**nginx 실행 및 자동시작 등록**
```bash
# 즉시 실행
brew services start nginx

# 정상 작동 확인
curl http://localhost:8787/works/001.jpg -I
```

`brew services start`로 등록하면 Mac이 재부팅되어도 nginx가 자동으로 다시 실행됩니다.

### 2-3. Mac 절전 방지 설정 (24시간 구동을 위해 필수)

Mac이 잠자기 모드로 들어가면 nginx/cloudflared 연결이 끊깁니다. 아래로 절전 비활성화:

```bash
# 시스템 절전 완전 비활성화 (전원 어댑터 연결 상태 기준)
sudo pmset -c sleep 0
sudo pmset -c disksleep 0
sudo pmset -c displaysleep 10   # 화면만 꺼지게 하고 싶다면 원하는 분 단위로 설정

# 설정 확인
pmset -g
```

> 데스크탑 Mac(mac mini, iMac 등)이라면 위 설정으로 충분합니다. 노트북(MacBook)이라면 **덮개를 닫아도 잠들지 않게** 별도 설정이 필요하니, 가급적 덮개를 열어두거나 외장 모니터에 연결해서 사용하세요.

---

## 3. Cloudflare Tunnel 설정

### 3-1. 사전 준비물
- 도메인 하나 (가비아, Namecheap 등에서 구매, 연 1만 원 안팎 — 이 부분만 유일하게 비용 발생 가능)
- Cloudflare 계정 (무료)

> 도메인이 전혀 없어도 Cloudflare가 임시로 제공하는 `trycloudflare.com` 서브도메인으로 테스트는 가능하지만, 이 방식은 매번 URL이 바뀌고 프로덕션용이 아니므로 실제 운영에는 정식 도메인 사용을 권장합니다.

### 3-2. 도메인을 Cloudflare에 연결
1. https://dash.cloudflare.com 접속 → **Add a Site**
2. 보유한 도메인 입력 → **Free 플랜** 선택
3. Cloudflare가 안내하는 네임서버 주소 2개 확인
4. 도메인을 구매한 등록업체(가비아 등) 관리 페이지에서 네임서버를 Cloudflare 것으로 변경
5. 반영까지 5분~24시간 소요 (보통 30분 이내)

### 3-3. cloudflared 설치 (Mac)

```bash
brew install cloudflare/cloudflare/cloudflared
```

### 3-4. Cloudflare 계정 인증

```bash
cloudflared tunnel login
```
→ 브라우저가 열리며 Cloudflare 로그인 및 도메인 선택 화면이 나타남 → 승인

### 3-5. 터널 생성

```bash
cloudflared tunnel create kinetic-art-tunnel
```

실행하면 터널 ID와 인증 정보(`.json` 파일)가 `~/.cloudflared/`에 생성됩니다.

### 3-6. 터널 설정 파일 작성

```bash
nano ~/.cloudflared/config.yml
```

내용:
```yaml
tunnel: kinetic-art-tunnel
credentials-file: /Users/[사용자명]/.cloudflared/[터널ID].json

ingress:
  - hostname: images.내도메인.com
    service: http://localhost:8787
  - service: http_status:404
```

### 3-7. DNS 라우팅 연결

```bash
cloudflared tunnel route dns kinetic-art-tunnel images.내도메인.com
```

### 3-8. 터널 실행 테스트

```bash
cloudflared tunnel run kinetic-art-tunnel
```

정상이면 터미널에 `Connection registered` 메시지가 출력됩니다. 이후 브라우저에서:
```
https://images.내도메인.com/works/001.jpg
```
로 접속해서 실제 이미지가 뜨는지 확인합니다.

### 3-9. 백그라운드 서비스로 등록 (24시간 자동 구동)

Mac을 재부팅해도 자동으로 터널이 다시 켜지도록 launchd 서비스로 등록:

```bash
sudo cloudflared service install
```

이후 아래 명령으로 상태 확인 가능:
```bash
sudo launchctl list | grep cloudflared
```

---

## 4. Supabase 프로젝트 깨어있게 유지하기 (선택, 권장)

Supabase 무료 프로젝트가 7일 비활성으로 일시정지되는 것을 막기 위해, 집 Mac에서 주기적으로 더미 요청을 보내는 스크립트를 등록합니다.

### 4-1. 핑 스크립트 작성

```bash
mkdir -p /Users/[사용자명]/kinetic-art-server/scripts
nano /Users/[사용자명]/kinetic-art-server/scripts/ping_supabase.sh
```

내용:
```bash
#!/bin/bash
curl -s "https://xxxxx.supabase.co/rest/v1/kinetic_artworks?select=id&limit=1" \
  -H "apikey: [anon public key]" \
  -H "Authorization: Bearer [anon public key]" \
  >> /Users/[사용자명]/kinetic-art-server/logs/ping.log 2>&1
```

```bash
chmod +x /Users/[사용자명]/kinetic-art-server/scripts/ping_supabase.sh
```

### 4-2. launchd로 매일 자동 실행 등록

```bash
nano ~/Library/LaunchAgents/com.kineticart.supabaseping.plist
```

내용:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.kineticart.supabaseping</string>
    <key>ProgramArguments</key>
    <array>
        <string>/Users/[사용자명]/kinetic-art-server/scripts/ping_supabase.sh</string>
    </array>
    <key>StartInterval</key>
    <integer>86400</integer>
    <key>RunAtLoad</key>
    <true/>
</dict>
</plist>
```

등록 및 실행:
```bash
launchctl load ~/Library/LaunchAgents/com.kineticart.supabaseping.plist
```

---

## 5. 보안 체크리스트

- [ ] Supabase `service_role key`는 절대 클라이언트/공개 저장소에 넣지 않기 (조회용은 `anon key`만 사용)
- [ ] `kinetic_artworks` 테이블에 RLS(Row Level Security) 활성화 확인
- [ ] nginx `autoindex off` 설정으로 폴더 목록 노출 방지 확인
- [ ] Cloudflare 대시보드 → **Zero Trust → Access** 에서 필요 시 접근 제한 정책(이메일 인증 등) 추가 가능 (완전 공개 갤러리라면 생략 가능)
- [ ] 원본 파일 백업: Supabase 무료 플랜은 DB 백업이 없고, 집 Mac도 단일 장애점이므로 **외장하드나 다른 클라우드에 원본 파일 이중 백업 권장**

---

## 6. 운영 중 모니터링 팁

| 확인 항목 | 방법 |
|---|---|
| Supabase DB 용량 | 대시보드 → Settings → Usage → Database Size |
| Supabase 대역폭(5GB/월) | 대시보드 → Settings → Usage → Bandwidth |
| Cloudflare Tunnel 연결 상태 | `cloudflared tunnel info kinetic-art-tunnel` |
| nginx 서비스 상태 | `brew services list` |
| Mac 절전 설정 확인 | `pmset -g` |

---

## 7. 전체 흐름 요약

1. 클라이언트가 Supabase REST API(`/rest/v1/kinetic_artworks`)로 작품 목록/메타데이터 조회
2. 응답에 포함된 `image_url`, `detail_text_url`은 `https://images.내도메인.com/...` 형태
3. 클라이언트가 이 URL로 직접 요청 → Cloudflare 엣지 → 아웃바운드 터널 → 집 Mac nginx → 원본 파일 응답
4. 집 Mac은 절전 없이 24시간 구동, cloudflared는 launchd로 자동 재시작
5. Supabase는 6일 주기 핑으로 자동 일시정지 방지

이 구성은 Supabase, Cloudflare Tunnel 모두 완전 무료이며, 유일하게 비용이 발생할 수 있는 부분은 도메인 구매(연 1만 원 안팎)뿐입니다.
